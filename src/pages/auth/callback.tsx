import {CallbackPanel} from "@/components/auth/callback-panel.tsx";

/** Where both providers return after a sign-in started from `/auth`. */
export const Callback = () => <CallbackPanel ns="auth"/>;
